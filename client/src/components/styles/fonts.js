import { Montserrat_Alternates } from "next/font/google";

// If loading a variable font, you don't need to specify the font weight
export const montserrat_Alternates = Montserrat_Alternates({ subsets: ['latin'],weight:['500', '600']})

import { Bagel_Fat_One} from 'next/font/google'
 
// If loading a variable font, you don't need to specify the font weight
// bagel requires a weight
export const bagel_Fat_One = Bagel_Fat_One({ subsets: ['latin'], weight: ['400'] })